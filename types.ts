export interface FeatureItem {
  id: string;
  title: string;
  description: string;
  image: string;
}

export interface SpecItem {
  label: string;
  value: string;
}

export interface Partner {
  name: string;
  role: string;
  company: string;
  quote: string;
}